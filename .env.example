# Supabase — cliente (navegador: login, auth). Usar solo estas en frontend.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase — servidor (DB, Storage). NUNCA exponer al cliente.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-only-server
SUPABASE_STORAGE_BUCKET=public

# Admin panel /admin: solo estos emails pueden iniciar sesión (whitelist).
ADMIN_EMAILS=admin@example.com

# Opcional: si el bucket no es público, las imágenes se sirven por signed URL (solo server).
