# Payload
PAYLOAD_SECRET=your-secret-min-32-chars-long-change-in-production

# Postgres (required for Payload)
DATABASE_URL=postgresql://user:password@localhost:5432/pablogoldberg

# S3 / R2 (optional – if set, media uploads go to S3/R2 instead of local disk)
# For Cloudflare R2: create a bucket, get API token, set endpoint to https://<ACCOUNT_ID>.r2.cloudflarestorage.com
# S3_BUCKET=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_REGION=auto
# S3_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com

# If using R2/S3 with a custom public URL for images, add that host to next.config.ts images.remotePatterns.
